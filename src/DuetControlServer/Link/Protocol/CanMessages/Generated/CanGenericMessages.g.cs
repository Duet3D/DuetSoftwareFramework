// <auto-generated>
//     Generated by src/DuetCanMessage.SourceGenerators from Schema/can-messages.json.
//     Do not edit this file directly: edit the schema and re-run the generator.
// </auto-generated>
#nullable enable

using System;
using System.Collections.Immutable;
using System.Runtime.InteropServices;
using DuetAPI.Commands;
using DuetControlServer.Link.Protocol.Shared;

namespace DuetControlServer.Link.Protocol.CanMessages;

/// <summary>
/// The M150 generic message.
///
/// Sent as <see cref="CanMessageType.WriteLedStrip" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M150Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM150 : ICanGenericMessage<CanMessageM150>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.WriteLedStrip;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M150Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'E' parameter, or null if the message does not carry it: strip number</summary>
    public ushort? E
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'E');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'E', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: number of LEDs to set</summary>
    public ushort? S
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it: red intensity</summary>
    public byte? R
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'U' parameter, or null if the message does not carry it: green intensity</summary>
    public byte? U
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'U');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'U', value);
    }

    /// <summary>The 'B' parameter, or null if the message does not carry it: blue intensity</summary>
    public byte? B
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'B');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'B', value);
    }

    /// <summary>The 'W' parameter, or null if the message does not carry it: white intensity</summary>
    public byte? W
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'W');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'W', value);
    }

    /// <summary>The 'P' parameter, or null if the message does not carry it: brightness 0-255</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'Y' parameter, or null if the message does not carry it: alternative brightness 0-31</summary>
    public byte? Y
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'Y');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'Y', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it: 'more follows' flag</summary>
    public byte? F
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }
}

/// <summary>
/// The M308V1 generic message.
///
/// Sent as <see cref="CanMessageType.M308V1" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M308V1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM308V1 : ICanGenericMessage<CanMessageM308V1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M308V1;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M308V1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'T' parameter, or null if the message does not carry it.</summary>
    public float? T
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'B' parameter, or null if the message does not carry it.</summary>
    public float? B
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'B');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'B', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public float? C
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public float? R
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'L' parameter, or null if the message does not carry it.</summary>
    public short? L
    {
        readonly get => (short?)CanGenericParser.GetInt(Generic, ParamTable, 'L');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'L', value);
    }

    /// <summary>The 'H' parameter, or null if the message does not carry it.</summary>
    public short? H
    {
        readonly get => (short?)CanGenericParser.GetInt(Generic, ParamTable, 'H');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'H', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it.</summary>
    public byte? F
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public byte? S
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'W' parameter, or null if the message does not carry it.</summary>
    public byte? W
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'W');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'W', value);
    }

    /// <summary>The 'K' parameter, or null if the message does not carry it.</summary>
    public char? K
    {
        readonly get => CanGenericParser.GetChar(Generic, ParamTable, 'K');
        set => CanGenericWriter.SetChar(ref Generic, ParamTable, 'K', value);
    }

    /// <summary>The 'Y' parameter, or null if the message does not carry it.</summary>
    public string? Y
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'Y');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'Y', value);
    }

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public string? P
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'U' parameter, or null if the message does not carry it.</summary>
    public float? U
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'U');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'U', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it.</summary>
    public float? V
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'V', value);
    }
}

/// <summary>
/// The M569 generic message.
///
/// Sent as <see cref="CanMessageType.M569" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569 : ICanGenericMessage<CanMessageM569>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public byte? S
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public sbyte? R
    {
        readonly get => (sbyte?)CanGenericParser.GetInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'D' parameter, or null if the message does not carry it.</summary>
    public byte? D
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'D');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'D', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it.</summary>
    public byte? F
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'B' parameter, or null if the message does not carry it.</summary>
    public byte? B
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'B');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'B', value);
    }

    /// <summary>The 'H' parameter, or null if the message does not carry it.</summary>
    public ushort? H
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'H');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'H', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it.</summary>
    public ushort? V
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'V', value);
    }

    /// <summary>The 'Y' parameter, or null if the message does not carry it.</summary>
    public uint[]? Y
    {
        readonly get => CanGenericParser.GetUIntArray(Generic, ParamTable, 'Y');
        set => CanGenericWriter.SetUIntArray(ref Generic, ParamTable, 'Y', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it.</summary>
    public float[]? T
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'U' parameter, or null if the message does not carry it.</summary>
    public sbyte? U
    {
        readonly get => (sbyte?)CanGenericParser.GetInt(Generic, ParamTable, 'U');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'U', value);
    }
}

/// <summary>
/// Configure closed loop stepper motor
///
/// Sent as <see cref="CanMessageType.M569P1" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point1 : ICanGenericMessage<CanMessageM569Point1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P1;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it.</summary>
    public byte? T
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'E' parameter, or null if the message does not carry it.</summary>
    public float[]? E
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'E');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'E', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public float? C
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public float? R
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'I' parameter, or null if the message does not carry it.</summary>
    public float? I
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'I');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'I', value);
    }

    /// <summary>The 'D' parameter, or null if the message does not carry it.</summary>
    public float? D
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'D');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'D', value);
    }

    /// <summary>The 'h' parameter, or null if the message does not carry it: was 'H', no longer used, position retained for backwards compatibility (using lowercase 'h' means it won't be matched)</summary>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public float? h
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'h');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'h', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: steps/rev added for EXP1HCL firmware 3.5 compatibility</summary>
    public ushort? S
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it: velocity feedforward term added in 3.5beta2</summary>
    public float? V
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'V', value);
    }

    /// <summary>The 'A' parameter, or null if the message does not carry it: acceleration feedforward term added in 3.5beta4</summary>
    public float? A
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'A');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'A', value);
    }

    /// <summary>The 'Q' parameter, or null if the message does not carry it: torque constant in Nm per A added in 3.5 post beta4</summary>
    public float? Q
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'Q');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'Q', value);
    }

    /// <summary>The 'Y' parameter, or null if the message does not carry it: magnetic encoder type, added at 3.6.2</summary>
    public string? Y
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'Y');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'Y', value);
    }
}

/// <summary>
/// Read or write stepper driver register
///
/// Sent as <see cref="CanMessageType.M569P2" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point2Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point2 : ICanGenericMessage<CanMessageM569Point2>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P2;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point2Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public byte? R
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it.</summary>
    public uint? V
    {
        readonly get => CanGenericParser.GetUInt(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'V', value);
    }
}

/// <summary>
/// Set driver torque mode
///
/// Sent as <see cref="CanMessageType.M569P4" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point4Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point4 : ICanGenericMessage<CanMessageM569Point4>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P4;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point4Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it: driver</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it: requested torque in Nm</summary>
    public float? T
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it: maximum speed to move at</summary>
    public float? V
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'V', value);
    }
}

/// <summary>
/// Note: M569Point6Params_StatusOnly must be kept in step with this!
///
/// Sent as <see cref="CanMessageType.M569P6" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point6Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point6 : ICanGenericMessage<CanMessageM569Point6>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point6Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it.</summary>
    public byte? V
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'V', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: speed parameter (in full steps/sec) for step tuning move (can't use V because it is taken already)</summary>
    public float? S
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'A' parameter, or null if the message does not carry it: acceleration parameter (in full steps/sec^2) for step tuning move</summary>
    public float? A
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'A');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'A', value);
    }
}

/// <summary>
/// This is the same as M569Point6Params except that it doesn't pick up the V parameter from the GCodeBuffer, and we don't need the extra parameters
///
/// Sent as <see cref="CanMessageType.M569P6" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point6Params_StatusOnly" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point6StatusOnly : ICanGenericMessage<CanMessageM569Point6StatusOnly>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point6Params_StatusOnly;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'v' parameter, or null if the message does not carry it: changed to lowercase so that we don't pick up this parameter from the GCode command</summary>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public byte? v
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'v');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'v', value);
    }
}

/// <summary>
/// The M569Point7 generic message.
///
/// Sent as <see cref="CanMessageType.M569P7" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point7Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point7 : ICanGenericMessage<CanMessageM569Point7>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P7;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point7Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it: drive number</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it: port name</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it: brake voltage added at 3.5beta2</summary>
    public float? V
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'V', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: brake off delay added at 3.6beta1</summary>
    public ushort? S
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }
}

/// <summary>
/// The M915 generic message.
///
/// Sent as <see cref="CanMessageType.M915" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M915Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM915 : ICanGenericMessage<CanMessageM915>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M915;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M915Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'd' parameter, or null if the message does not carry it: this is the bitmap of driver numbers to change the parameters for</summary>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public ushort? d
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'd');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'd', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public sbyte? S
    {
        readonly get => (sbyte?)CanGenericParser.GetInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it.</summary>
    public byte? F
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'H' parameter, or null if the message does not carry it.</summary>
    public ushort? H
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'H');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'H', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it.</summary>
    public ushort? T
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public byte? R
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'R', value);
    }
}

/// <summary>
/// The M950Heater generic message.
///
/// Sent as <see cref="CanMessageType.M950Heater" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950HeaterParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Heater : ICanGenericMessage<CanMessageM950Heater>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Heater;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950HeaterParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'H' parameter, or null if the message does not carry it.</summary>
    public ushort? H
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'H');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'H', value);
    }

    /// <summary>The 'Q' parameter, or null if the message does not carry it.</summary>
    public ushort? Q
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'Q');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'Q', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it.</summary>
    public ushort? T
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'B' parameter, or null if the message does not carry it: ambient temperature sensor number, added at 3.7.0-beta.2</summary>
    public short? B
    {
        readonly get => (short?)CanGenericParser.GetInt(Generic, ParamTable, 'B');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'B', value);
    }
}

/// <summary>
/// The M950Fan generic message.
///
/// Sent as <see cref="CanMessageType.M950Fan" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950FanParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Fan : ICanGenericMessage<CanMessageM950Fan>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Fan;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950FanParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'F' parameter, or null if the message does not carry it.</summary>
    public ushort? F
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'Q' parameter, or null if the message does not carry it.</summary>
    public ushort? Q
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'Q');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'Q', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'K' parameter, or null if the message does not carry it: tacho pulses/rev added at 3.5</summary>
    public float? K
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'K');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'K', value);
    }
}

/// <summary>
/// The M950Gpio generic message.
///
/// Sent as <see cref="CanMessageType.M950Gpio" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950GpioParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Gpio : ICanGenericMessage<CanMessageM950Gpio>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Gpio;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950GpioParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public ushort? P
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'Q' parameter, or null if the message does not carry it.</summary>
    public ushort? Q
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'Q');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'Q', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: 1 if servo, 0 if GPIO</summary>
    public byte? S
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }
}

/// <summary>
/// The M950Led generic message.
///
/// Sent as <see cref="CanMessageType.M950Led" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950LedParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Led : ICanGenericMessage<CanMessageM950Led>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Led;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950LedParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'E' parameter, or null if the message does not carry it: Strip number</summary>
    public ushort? E
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'E');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'E', value);
    }

    /// <summary>The 'U' parameter, or null if the message does not carry it: Maximum number of LEDs</summary>
    public ushort? U
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'U');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'U', value);
    }

    /// <summary>The 'Q' parameter, or null if the message does not carry it: SPI frequency (DotStar) or cycle frequency (Neopixel)</summary>
    public uint? Q
    {
        readonly get => CanGenericParser.GetUInt(Generic, ParamTable, 'Q');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'Q', value);
    }

    /// <summary>The 'T' parameter, or null if the message does not carry it: Type (0 = DotStar, 1 = RGB Neopixel, 2 = RGBW Neopixel)</summary>
    public byte? T
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it: Port name</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'L' parameter, or null if the message does not carry it: Timing parameters, used by STM implementation only, implies bit-banged if present</summary>
    public uint[]? L
    {
        readonly get => CanGenericParser.GetUIntArray(Generic, ParamTable, 'L');
        set => CanGenericWriter.SetUIntArray(ref Generic, ParamTable, 'L', value);
    }

    /// <summary>The 'K' parameter, or null if the message does not carry it.</summary>
    public byte? K
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'K');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'K', value);
    }
}

/// <summary>
/// This must cover all parameters used by any supported type of filament monitor, except for the type and extruder number
///
/// Sent as <see cref="CanMessageType.ConfigureFilamentMonitor" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.ConfigureFilamentMonitorParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageConfigureFilamentMonitor : ICanGenericMessage<CanMessageConfigureFilamentMonitor>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.ConfigureFilamentMonitor;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.ConfigureFilamentMonitorParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'd' parameter, or null if the message does not carry it: this is the driver number on the remote board</summary>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public byte? d
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'd');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'd', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public byte? S
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'A' parameter, or null if the message does not carry it.</summary>
    public byte? A
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'A');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'A', value);
    }

    /// <summary>The 'L' parameter, or null if the message does not carry it.</summary>
    public float? L
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'L');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'L', value);
    }

    /// <summary>The 'E' parameter, or null if the message does not carry it.</summary>
    public float? E
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'E');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'E', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public uint[]? R
    {
        readonly get => CanGenericParser.GetUIntArray(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetUIntArray(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }
}

/// <summary>
/// Accelerometer settings
///
/// Sent as <see cref="CanMessageType.AccelerometerConfig" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M955Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM955 : ICanGenericMessage<CanMessageM955>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.AccelerometerConfig;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M955Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'P' parameter, or null if the message does not carry it: accelerometer number</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetDriverId(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'I' parameter, or null if the message does not carry it: orientation</summary>
    public byte? I
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'I');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'I', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it: resolution (bits)</summary>
    public byte? R
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it: sampling rate</summary>
    public ushort? S
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }
}

/// <summary>
/// M122 P1 parameters
///
/// Sent as <see cref="CanMessageType.TestReport" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M122P1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM122P1 : ICanGenericMessage<CanMessageM122P1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.TestReport;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M122P1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'T' parameter, or null if the message does not carry it: MCU temperature limits</summary>
    public float[]? T
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'T', value);
    }

    /// <summary>The 'V' parameter, or null if the message does not carry it: VIN voltage limits</summary>
    public float[]? V
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'V');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'V', value);
    }

    /// <summary>The 'W' parameter, or null if the message does not carry it: V12 voltage limits</summary>
    public float[]? W
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'W');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'W', value);
    }

    /// <summary>The 'U' parameter, or null if the message does not carry it: thermistor temperature reading limits</summary>
    public float[]? U
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'U');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'U', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it: inductive sensor frequency limits</summary>
    public float[]? F
    {
        readonly get => CanGenericParser.GetFloatArray(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetFloatArray(ref Generic, ParamTable, 'F', value);
    }
}

/// <summary>
/// M655 parameters
///
/// Sent as <see cref="CanMessageType.M655" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M655Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM655 : ICanGenericMessage<CanMessageM655>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M655;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M655Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'E' parameter, or null if the message does not carry it.</summary>
    public float? E
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'E');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'E', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it.</summary>
    public float? F
    {
        readonly get => CanGenericParser.GetFloat(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetFloat(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'R' parameter, or null if the message does not carry it.</summary>
    public int? R
    {
        readonly get => CanGenericParser.GetInt(Generic, ParamTable, 'R');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'R', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public int? S
    {
        readonly get => CanGenericParser.GetInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetInt(ref Generic, ParamTable, 'S', value);
    }

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public ushort? P
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'C' parameter, or null if the message does not carry it.</summary>
    public string? C
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'C');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'C', value);
    }

    /// <summary>The 'A' parameter, or null if the message does not carry it.</summary>
    public string? A
    {
        readonly get => CanGenericParser.GetString(Generic, ParamTable, 'A');
        set => CanGenericWriter.SetString(ref Generic, ParamTable, 'A', value);
    }
}

/// <summary>
/// M111 parameters
///
/// Sent as <see cref="CanMessageType.M111" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M111Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM111 : ICanGenericMessage<CanMessageM111>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M111;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M111Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'D' parameter, or null if the message does not carry it.</summary>
    public uint? D
    {
        readonly get => CanGenericParser.GetUInt(Generic, ParamTable, 'D');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'D', value);
    }

    /// <summary>The 'F' parameter, or null if the message does not carry it: probably not needed but include it in case</summary>
    public ushort? F
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'F');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'F', value);
    }

    /// <summary>The 'P' parameter, or null if the message does not carry it.</summary>
    public byte? P
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'P');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'P', value);
    }

    /// <summary>The 'S' parameter, or null if the message does not carry it.</summary>
    public byte? S
    {
        readonly get => (byte?)CanGenericParser.GetUInt(Generic, ParamTable, 'S');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'S', value);
    }
}

/// <summary>
/// M959 parameters. The B parameter selects the board and is consumed by the main board
///
/// Sent as <see cref="CanMessageType.SetConnectionTimeout" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M959Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are.
///
/// Every parameter is optional, so each is a nullable property: it reads back null when the message
/// is not carrying that parameter, and assigning null takes it back out. They may be set in any
/// order, and taken from a G-code command with <see cref="FromCode" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM959 : ICanGenericMessage<CanMessageM959>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.SetConnectionTimeout;

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.ParamTable" />
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M959Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength() => CanGenericLayout.ActualDataLength(Generic.Data, Generic.ParamMap, ParamTable);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.FromCode" />
    public void FromCode(Code code) => CanGenericWriter.FromCode(ref Generic, ParamTable, code);

    /// <inheritdoc cref="ICanGenericMessage{TSelf}.Clear" />
    public void Clear() => Generic = default;

    /// <summary>The 'T' parameter, or null if the message does not carry it: connection timeout in seconds</summary>
    public ushort? T
    {
        readonly get => (ushort?)CanGenericParser.GetUInt(Generic, ParamTable, 'T');
        set => CanGenericWriter.SetUInt(ref Generic, ParamTable, 'T', value);
    }
}

