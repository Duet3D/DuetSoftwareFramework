// <auto-generated>
//     Generated by tools/CanMessageGenerator from Schema/can-messages.json.
//     Do not edit this file directly: edit the schema and re-run the generator.
// </auto-generated>
#nullable enable

using System;
using System.Collections.Immutable;
using System.Runtime.InteropServices;
using DuetControlServer.Link.Protocol.Shared;

namespace DuetControlServer.Link.Protocol.CanMessages;

/// <summary>
/// The M150 generic message.
///
/// Sent as <see cref="CanMessageType.WriteLedStrip" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M150Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M150Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM150 : ICanMessage<CanMessageM150>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.WriteLedStrip;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M150Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M150Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M150 generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M150Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M150Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.WriteLedStrip;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM150 Message
    {
        get
        {
            CanMessageM150 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'E' parameter: strip number</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder E(ushort value) { _writer.AddUInt('E', value); return this; }

    /// <summary>Set the 'S' parameter: number of LEDs to set</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder S(ushort value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'R' parameter: red intensity</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder R(byte value) { _writer.AddUInt('R', value); return this; }

    /// <summary>Set the 'U' parameter: green intensity</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder U(byte value) { _writer.AddUInt('U', value); return this; }

    /// <summary>Set the 'B' parameter: blue intensity</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder B(byte value) { _writer.AddUInt('B', value); return this; }

    /// <summary>Set the 'W' parameter: white intensity</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder W(byte value) { _writer.AddUInt('W', value); return this; }

    /// <summary>Set the 'P' parameter: brightness 0-255</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder P(byte value) { _writer.AddUInt('P', value); return this; }

    /// <summary>Set the 'Y' parameter: alternative brightness 0-31</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder Y(byte value) { _writer.AddUInt('Y', value); return this; }

    /// <summary>Set the 'F' parameter: 'more follows' flag</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M150Builder F(byte value) { _writer.AddUInt('F', value); return this; }
}

/// <summary>
/// The M308V1 generic message.
///
/// Sent as <see cref="CanMessageType.M308V1" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M308V1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M308V1Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM308V1 : ICanMessage<CanMessageM308V1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M308V1;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M308V1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M308V1Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M308V1 generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M308V1Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M308V1Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M308V1;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM308V1 Message
    {
        get
        {
            CanMessageM308V1 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'T' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder T(float value) { _writer.AddFloat('T', value); return this; }

    /// <summary>Set the 'B' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder B(float value) { _writer.AddFloat('B', value); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder C(float value) { _writer.AddFloat('C', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder R(float value) { _writer.AddFloat('R', value); return this; }

    /// <summary>Set the 'L' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder L(short value) { _writer.AddInt('L', value); return this; }

    /// <summary>Set the 'H' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder H(short value) { _writer.AddInt('H', value); return this; }

    /// <summary>Set the 'F' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder F(byte value) { _writer.AddUInt('F', value); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder S(byte value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'W' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder W(byte value) { _writer.AddUInt('W', value); return this; }

    /// <summary>Set the 'K' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder K(char value) { _writer.AddChar('K', value); return this; }

    /// <summary>Set the 'Y' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder Y(string value) { _writer.AddString('Y', value); return this; }

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder P(string value) { _writer.AddString('P', value); return this; }

    /// <summary>Set the 'U' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder U(float value) { _writer.AddFloat('U', value); return this; }

    /// <summary>Set the 'V' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M308V1Builder V(float value) { _writer.AddFloat('V', value); return this; }
}

/// <summary>
/// The M569 generic message.
///
/// Sent as <see cref="CanMessageType.M569" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569 : ICanMessage<CanMessageM569>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M569 generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569 Message
    {
        get
        {
            CanMessageM569 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder S(byte value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder R(sbyte value) { _writer.AddInt('R', value); return this; }

    /// <summary>Set the 'D' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder D(byte value) { _writer.AddUInt('D', value); return this; }

    /// <summary>Set the 'F' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder F(byte value) { _writer.AddUInt('F', value); return this; }

    /// <summary>Set the 'B' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder B(byte value) { _writer.AddUInt('B', value); return this; }

    /// <summary>Set the 'H' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder H(ushort value) { _writer.AddUInt('H', value); return this; }

    /// <summary>Set the 'V' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder V(ushort value) { _writer.AddUInt('V', value); return this; }

    /// <summary>Set the 'Y' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder Y(params ReadOnlySpan<uint> values) { _writer.AddUIntArray('Y', values); return this; }

    /// <summary>Set the 'T' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder T(params ReadOnlySpan<float> values) { _writer.AddFloatArray('T', values); return this; }

    /// <summary>Set the 'U' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Builder U(sbyte value) { _writer.AddInt('U', value); return this; }
}

/// <summary>
/// Configure closed loop stepper motor
///
/// Sent as <see cref="CanMessageType.M569P1" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point1Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point1 : ICanMessage<CanMessageM569Point1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P1;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point1Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Configure closed loop stepper motor
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point1Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point1Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P1;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point1 Message
    {
        get
        {
            CanMessageM569Point1 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'T' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder T(byte value) { _writer.AddUInt('T', value); return this; }

    /// <summary>Set the 'E' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder E(params ReadOnlySpan<float> values) { _writer.AddFloatArray('E', values); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder C(float value) { _writer.AddFloat('C', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder R(float value) { _writer.AddFloat('R', value); return this; }

    /// <summary>Set the 'I' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder I(float value) { _writer.AddFloat('I', value); return this; }

    /// <summary>Set the 'D' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder D(float value) { _writer.AddFloat('D', value); return this; }

    /// <summary>Set the 'h' parameter: was 'H', no longer used, position retained for backwards compatibility (using lowercase 'h' means it won't be matched)</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public M569Point1Builder h(float value) { _writer.AddFloat('h', value); return this; }

    /// <summary>Set the 'S' parameter: steps/rev added for EXP1HCL firmware 3.5 compatibility</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder S(ushort value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'V' parameter: velocity feedforward term added in 3.5beta2</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder V(float value) { _writer.AddFloat('V', value); return this; }

    /// <summary>Set the 'A' parameter: acceleration feedforward term added in 3.5beta4</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder A(float value) { _writer.AddFloat('A', value); return this; }

    /// <summary>Set the 'Q' parameter: torque constant in Nm per A added in 3.5 post beta4</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder Q(float value) { _writer.AddFloat('Q', value); return this; }

    /// <summary>Set the 'Y' parameter: magnetic encoder type, added at 3.6.2</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point1Builder Y(string value) { _writer.AddString('Y', value); return this; }
}

/// <summary>
/// Read or write stepper driver register
///
/// Sent as <see cref="CanMessageType.M569P2" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point2Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point2Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point2 : ICanMessage<CanMessageM569Point2>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P2;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point2Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point2Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Read or write stepper driver register
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point2Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point2Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P2;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point2 Message
    {
        get
        {
            CanMessageM569Point2 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point2Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point2Builder R(byte value) { _writer.AddUInt('R', value); return this; }

    /// <summary>Set the 'V' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point2Builder V(uint value) { _writer.AddUInt('V', value); return this; }
}

/// <summary>
/// Set driver torque mode
///
/// Sent as <see cref="CanMessageType.M569P4" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point4Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point4Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point4 : ICanMessage<CanMessageM569Point4>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P4;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point4Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point4Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Set driver torque mode
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point4Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point4Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P4;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point4 Message
    {
        get
        {
            CanMessageM569Point4 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter: driver</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point4Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'T' parameter: requested torque in Nm</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point4Builder T(float value) { _writer.AddFloat('T', value); return this; }

    /// <summary>Set the 'V' parameter: maximum speed to move at</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point4Builder V(float value) { _writer.AddFloat('V', value); return this; }
}

/// <summary>
/// Note: M569Point6Params_StatusOnly must be kept in step with this!
///
/// Sent as <see cref="CanMessageType.M569P6" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point6Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point6Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point6 : ICanMessage<CanMessageM569Point6>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point6Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point6Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Note: M569Point6Params_StatusOnly must be kept in step with this!
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point6Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point6Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point6 Message
    {
        get
        {
            CanMessageM569Point6 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point6Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'V' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point6Builder V(byte value) { _writer.AddUInt('V', value); return this; }

    /// <summary>Set the 'S' parameter: speed parameter (in full steps/sec) for step tuning move (can't use V because it is taken already)</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point6Builder S(float value) { _writer.AddFloat('S', value); return this; }

    /// <summary>Set the 'A' parameter: acceleration parameter (in full steps/sec^2) for step tuning move</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point6Builder A(float value) { _writer.AddFloat('A', value); return this; }
}

/// <summary>
/// This is the same as M569Point6Params except that it doesn't pick up the V parameter from the GCodeBuffer, and we don't need the extra parameters
///
/// Sent as <see cref="CanMessageType.M569P6" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point6Params_StatusOnly" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point6StatusOnlyBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point6StatusOnly : ICanMessage<CanMessageM569Point6StatusOnly>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point6Params_StatusOnly;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point6Params_StatusOnly);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// This is the same as M569Point6Params except that it doesn't pick up the V parameter from the GCodeBuffer, and we don't need the extra parameters
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point6StatusOnlyBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point6Params_StatusOnly);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P6;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point6StatusOnly Message
    {
        get
        {
            CanMessageM569Point6StatusOnly message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point6StatusOnlyBuilder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'v' parameter: changed to lowercase so that we don't pick up this parameter from the GCode command</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public M569Point6StatusOnlyBuilder v(byte value) { _writer.AddUInt('v', value); return this; }
}

/// <summary>
/// The M569Point7 generic message.
///
/// Sent as <see cref="CanMessageType.M569P7" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M569Point7Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M569Point7Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM569Point7 : ICanMessage<CanMessageM569Point7>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M569P7;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M569Point7Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M569Point7Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M569Point7 generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M569Point7Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M569Point7Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M569P7;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM569Point7 Message
    {
        get
        {
            CanMessageM569Point7 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter: drive number</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point7Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'C' parameter: port name</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point7Builder C(string value) { _writer.AddString('C', value); return this; }

    /// <summary>Set the 'V' parameter: brake voltage added at 3.5beta2</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point7Builder V(float value) { _writer.AddFloat('V', value); return this; }

    /// <summary>Set the 'S' parameter: brake off delay added at 3.6beta1</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M569Point7Builder S(ushort value) { _writer.AddUInt('S', value); return this; }
}

/// <summary>
/// The M915 generic message.
///
/// Sent as <see cref="CanMessageType.M915" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M915Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M915Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM915 : ICanMessage<CanMessageM915>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M915;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M915Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M915Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M915 generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M915Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M915Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M915;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM915 Message
    {
        get
        {
            CanMessageM915 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'd' parameter: this is the bitmap of driver numbers to change the parameters for</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public M915Builder d(ushort value) { _writer.AddUInt('d', value); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M915Builder S(sbyte value) { _writer.AddInt('S', value); return this; }

    /// <summary>Set the 'F' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M915Builder F(byte value) { _writer.AddUInt('F', value); return this; }

    /// <summary>Set the 'H' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M915Builder H(ushort value) { _writer.AddUInt('H', value); return this; }

    /// <summary>Set the 'T' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M915Builder T(ushort value) { _writer.AddUInt('T', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M915Builder R(byte value) { _writer.AddUInt('R', value); return this; }
}

/// <summary>
/// The M950Heater generic message.
///
/// Sent as <see cref="CanMessageType.M950Heater" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950HeaterParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M950HeaterBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Heater : ICanMessage<CanMessageM950Heater>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Heater;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950HeaterParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M950HeaterParams);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M950Heater generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M950HeaterBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M950HeaterParams);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M950Heater;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM950Heater Message
    {
        get
        {
            CanMessageM950Heater message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'H' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950HeaterBuilder H(ushort value) { _writer.AddUInt('H', value); return this; }

    /// <summary>Set the 'Q' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950HeaterBuilder Q(ushort value) { _writer.AddUInt('Q', value); return this; }

    /// <summary>Set the 'T' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950HeaterBuilder T(ushort value) { _writer.AddUInt('T', value); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950HeaterBuilder C(string value) { _writer.AddString('C', value); return this; }

    /// <summary>Set the 'B' parameter: ambient temperature sensor number, added at 3.7.0-beta.2</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950HeaterBuilder B(short value) { _writer.AddInt('B', value); return this; }
}

/// <summary>
/// The M950Fan generic message.
///
/// Sent as <see cref="CanMessageType.M950Fan" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950FanParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M950FanBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Fan : ICanMessage<CanMessageM950Fan>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Fan;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950FanParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M950FanParams);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M950Fan generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M950FanBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M950FanParams);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M950Fan;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM950Fan Message
    {
        get
        {
            CanMessageM950Fan message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'F' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950FanBuilder F(ushort value) { _writer.AddUInt('F', value); return this; }

    /// <summary>Set the 'Q' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950FanBuilder Q(ushort value) { _writer.AddUInt('Q', value); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950FanBuilder C(string value) { _writer.AddString('C', value); return this; }

    /// <summary>Set the 'K' parameter: tacho pulses/rev added at 3.5</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950FanBuilder K(float value) { _writer.AddFloat('K', value); return this; }
}

/// <summary>
/// The M950Gpio generic message.
///
/// Sent as <see cref="CanMessageType.M950Gpio" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950GpioParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M950GpioBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Gpio : ICanMessage<CanMessageM950Gpio>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Gpio;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950GpioParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M950GpioParams);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M950Gpio generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M950GpioBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M950GpioParams);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M950Gpio;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM950Gpio Message
    {
        get
        {
            CanMessageM950Gpio message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950GpioBuilder P(ushort value) { _writer.AddUInt('P', value); return this; }

    /// <summary>Set the 'Q' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950GpioBuilder Q(ushort value) { _writer.AddUInt('Q', value); return this; }

    /// <summary>Set the 'S' parameter: 1 if servo, 0 if GPIO</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950GpioBuilder S(byte value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950GpioBuilder C(string value) { _writer.AddString('C', value); return this; }
}

/// <summary>
/// The M950Led generic message.
///
/// Sent as <see cref="CanMessageType.M950Led" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M950LedParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M950LedBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM950Led : ICanMessage<CanMessageM950Led>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M950Led;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M950LedParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M950LedParams);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Builds the M950Led generic message.
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M950LedBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M950LedParams);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M950Led;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM950Led Message
    {
        get
        {
            CanMessageM950Led message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'E' parameter: Strip number</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder E(ushort value) { _writer.AddUInt('E', value); return this; }

    /// <summary>Set the 'U' parameter: Maximum number of LEDs</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder U(ushort value) { _writer.AddUInt('U', value); return this; }

    /// <summary>Set the 'Q' parameter: SPI frequency (DotStar) or cycle frequency (Neopixel)</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder Q(uint value) { _writer.AddUInt('Q', value); return this; }

    /// <summary>Set the 'T' parameter: Type (0 = DotStar, 1 = RGB Neopixel, 2 = RGBW Neopixel)</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder T(byte value) { _writer.AddUInt('T', value); return this; }

    /// <summary>Set the 'C' parameter: Port name</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder C(string value) { _writer.AddString('C', value); return this; }

    /// <summary>Set the 'L' parameter: Timing parameters, used by STM implementation only, implies bit-banged if present</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder L(params ReadOnlySpan<uint> values) { _writer.AddUIntArray('L', values); return this; }

    /// <summary>Set the 'K' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M950LedBuilder K(byte value) { _writer.AddUInt('K', value); return this; }
}

/// <summary>
/// This must cover all parameters used by any supported type of filament monitor, except for the type and extruder number
///
/// Sent as <see cref="CanMessageType.ConfigureFilamentMonitor" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.ConfigureFilamentMonitorParams" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="ConfigureFilamentMonitorBuilder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageConfigureFilamentMonitor : ICanMessage<CanMessageConfigureFilamentMonitor>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.ConfigureFilamentMonitor;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.ConfigureFilamentMonitorParams;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.ConfigureFilamentMonitorParams);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// This must cover all parameters used by any supported type of filament monitor, except for the type and extruder number
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class ConfigureFilamentMonitorBuilder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.ConfigureFilamentMonitorParams);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.ConfigureFilamentMonitor;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageConfigureFilamentMonitor Message
    {
        get
        {
            CanMessageConfigureFilamentMonitor message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'd' parameter: this is the driver number on the remote board</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    /// <remarks>Outside A..Z, so a G-code command can never supply this parameter; the caller has to.</remarks>
    public ConfigureFilamentMonitorBuilder d(byte value) { _writer.AddUInt('d', value); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder S(byte value) { _writer.AddUInt('S', value); return this; }

    /// <summary>Set the 'A' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder A(byte value) { _writer.AddUInt('A', value); return this; }

    /// <summary>Set the 'L' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder L(float value) { _writer.AddFloat('L', value); return this; }

    /// <summary>Set the 'E' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder E(float value) { _writer.AddFloat('E', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder R(params ReadOnlySpan<uint> values) { _writer.AddUIntArray('R', values); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public ConfigureFilamentMonitorBuilder C(string value) { _writer.AddString('C', value); return this; }
}

/// <summary>
/// Accelerometer settings
///
/// Sent as <see cref="CanMessageType.AccelerometerConfig" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M955Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M955Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM955 : ICanMessage<CanMessageM955>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.AccelerometerConfig;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M955Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M955Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// Accelerometer settings
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M955Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M955Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.AccelerometerConfig;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM955 Message
    {
        get
        {
            CanMessageM955 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'P' parameter: accelerometer number</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M955Builder P(byte localDriver) { _writer.AddDriverId('P', localDriver); return this; }

    /// <summary>Set the 'I' parameter: orientation</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M955Builder I(byte value) { _writer.AddUInt('I', value); return this; }

    /// <summary>Set the 'R' parameter: resolution (bits)</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M955Builder R(byte value) { _writer.AddUInt('R', value); return this; }

    /// <summary>Set the 'S' parameter: sampling rate</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M955Builder S(ushort value) { _writer.AddUInt('S', value); return this; }
}

/// <summary>
/// M122 P1 parameters
///
/// Sent as <see cref="CanMessageType.TestReport" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M122P1Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M122P1Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM122P1 : ICanMessage<CanMessageM122P1>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.TestReport;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M122P1Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M122P1Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// M122 P1 parameters
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M122P1Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M122P1Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.TestReport;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM122P1 Message
    {
        get
        {
            CanMessageM122P1 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'T' parameter: MCU temperature limits</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M122P1Builder T(params ReadOnlySpan<float> values) { _writer.AddFloatArray('T', values); return this; }

    /// <summary>Set the 'V' parameter: VIN voltage limits</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M122P1Builder V(params ReadOnlySpan<float> values) { _writer.AddFloatArray('V', values); return this; }

    /// <summary>Set the 'W' parameter: V12 voltage limits</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M122P1Builder W(params ReadOnlySpan<float> values) { _writer.AddFloatArray('W', values); return this; }

    /// <summary>Set the 'U' parameter: thermistor temperature reading limits</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M122P1Builder U(params ReadOnlySpan<float> values) { _writer.AddFloatArray('U', values); return this; }

    /// <summary>Set the 'F' parameter: inductive sensor frequency limits</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M122P1Builder F(params ReadOnlySpan<float> values) { _writer.AddFloatArray('F', values); return this; }
}

/// <summary>
/// M655 parameters
///
/// Sent as <see cref="CanMessageType.M655" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M655Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M655Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM655 : ICanMessage<CanMessageM655>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M655;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M655Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M655Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// M655 parameters
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M655Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M655Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M655;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM655 Message
    {
        get
        {
            CanMessageM655 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'E' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder E(float value) { _writer.AddFloat('E', value); return this; }

    /// <summary>Set the 'F' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder F(float value) { _writer.AddFloat('F', value); return this; }

    /// <summary>Set the 'R' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder R(int value) { _writer.AddInt('R', value); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder S(int value) { _writer.AddInt('S', value); return this; }

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder P(ushort value) { _writer.AddUInt('P', value); return this; }

    /// <summary>Set the 'C' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder C(string value) { _writer.AddString('C', value); return this; }

    /// <summary>Set the 'A' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M655Builder A(string value) { _writer.AddString('A', value); return this; }
}

/// <summary>
/// M111 parameters
///
/// Sent as <see cref="CanMessageType.M111" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M111Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M111Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM111 : ICanMessage<CanMessageM111>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.M111;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M111Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M111Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// M111 parameters
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M111Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M111Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.M111;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM111 Message
    {
        get
        {
            CanMessageM111 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'D' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M111Builder D(uint value) { _writer.AddUInt('D', value); return this; }

    /// <summary>Set the 'F' parameter: probably not needed but include it in case</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M111Builder F(ushort value) { _writer.AddUInt('F', value); return this; }

    /// <summary>Set the 'P' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M111Builder P(byte value) { _writer.AddUInt('P', value); return this; }

    /// <summary>Set the 'S' parameter.</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M111Builder S(byte value) { _writer.AddUInt('S', value); return this; }
}

/// <summary>
/// M959 parameters. The B parameter selects the board and is consumed by the main board
///
/// Sent as <see cref="CanMessageType.SetConnectionTimeout" />.
/// Carries a <see cref="CanMessageGeneric" /> body: the parameters of
/// <see cref="CanGenericTables.M959Params" /> that are being sent, packed in table order, plus a
/// bitmap saying which those are. Build one with <see cref="M959Builder" />.
/// </summary>
[StructLayout(LayoutKind.Explicit, Pack = 1, Size = 64)]
public struct CanMessageM959 : ICanMessage<CanMessageM959>
{
    /// <summary>The generic message body, in the format the expansion board reads</summary>
    [FieldOffset(0)] public CanMessageGeneric Generic;

    /// <inheritdoc cref="ICanMessage{TSelf}.MessageType" />
    public static CanMessageType MessageType => CanMessageType.SetConnectionTimeout;

    /// <summary>The parameter table this message is built against</summary>
    public static ImmutableArray<CanParamDescriptor> ParamTable => CanGenericTables.M959Params;

    /// <summary>
    /// Number of payload bytes this message occupies: the parameters actually present, plus the
    /// request ID and parameter map.
    ///
    /// The parameter map and the table are enough to work this out, because each present parameter's
    /// size follows from its table entry and, for the variable-length ones, from the data itself.
    /// </summary>
    public readonly uint GetActualDataLength()
    {
        int dataLength = CanGenericLayout.DataLength(Generic.Data, Generic.ParamMap, CanGenericTables.M959Params);
        return CanMessageGeneric.GetActualDataLength((uint)dataLength);
    }
}

/// <summary>
/// M959 parameters. The B parameter selects the board and is consumed by the main board
///
/// Every parameter is optional: only the ones set are sent, and the receiver is told which by the
/// message's parameter map. Parameters may be set in any order.
/// </summary>
public sealed class M959Builder
{
    private readonly CanGenericWriter _writer = new(CanGenericTables.M959Params);

    /// <summary>The message type this message is sent under</summary>
    public static CanMessageType MessageType => CanMessageType.SetConnectionTimeout;

    /// <summary>The message as built so far, ready to hand to the CAN send path.</summary>
    public CanMessageM959 Message
    {
        get
        {
            CanMessageM959 message = default;
            message.Generic = _writer.Message;
            return message;
        }
    }

    /// <summary>The generic message body on its own, without the type that identifies it</summary>
    public CanMessageGeneric Body => _writer.Message;

    /// <summary>Number of payload bytes to transmit, i.e. the parameters plus the request ID and parameter map</summary>
    public uint ActualDataLength => _writer.ActualDataLength;

    /// <summary>Set the 'T' parameter: connection timeout in seconds</summary>
    /// <returns>This builder, so that calls can be chained.</returns>
    public M959Builder T(ushort value) { _writer.AddUInt('T', value); return this; }
}

