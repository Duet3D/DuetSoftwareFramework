// <auto-generated>
//     Generated by tools/CanMessageGenerator from Schema/can-messages.json.
//     Do not edit this file directly: edit the schema and re-run the generator, otherwise
//     these values will drift away from CANlib's GCodeResult.h, which nothing else would catch.
// </auto-generated>

namespace DuetControlServer.Link.Protocol.Shared;

/// <summary>
/// Result of attempting to process a G-code command.
/// Mirrors GCodeResult in CANlib's GCodeResult.h. The value travels on the wire as the 4-bit
/// resultCode of a standard reply, so it has to keep CANlib's numbering.
/// </summary>
public enum CodeResult : byte
{
    /// <summary>we haven't finished processing this command</summary>
    NotFinished = 0,

    /// <summary>we have finished processing this code in the current state, and if the GCodeState is 'normal' then we have finished it completely</summary>
    Ok = 1,

    /// <summary>the command succeeded but a warning was generated</summary>
    Warning = 2,

    /// <summary>the command is not supported, but for this command we issue a warning not an error</summary>
    WarningNotSupported = 3,

    /// <summary>general error, the reason will be written to the associated reply buffer</summary>
    Error = 4,

    /// <summary>this command is not supported, and we treat it as an error because it's an important command</summary>
    ErrorNotSupported = 5,

    /// <summary>this command is supported in a different mode (FDM/Laser/CNC)</summary>
    NotSupportedInCurrentMode = 6,

    /// <summary>we are halted because of an emergency stop</summary>
    Stopped = 7,

    /// <summary>a critical parameter is missing or has a bad value</summary>
    BadOrMissingParameter = 8,

    /// <summary>only used if CAN expansion is supported - can be sent by expansion boards, so don't change its number!</summary>
    RemoteInternalError = 9,
    M291Cancelled = 10,
    /// <summary>we are waiting for a message box to be acknowledged so the command has been ignored</summary>
    WaitingForAckSoIgnored = 11,

    /// <summary>only used if CAN expansion is supported - we failed to allocate a CAN buffer to send a message to an expansion board</summary>
    NoCanBuffer = 12,

    /// <summary>only used if CAN expansion is supported - timed out waiting for a response to a CAN message - the associated reply buffer may contain more info</summary>
    CanResponseTimeout = 13,
}
